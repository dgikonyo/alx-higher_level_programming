#ifndef LISTS_H
#define LISTS_H

#include <stdlib.h>
#include <stdio.h>
#include <unistd.h>

/**
 * struct listint_t - singly linked list
 * @head: points to the next node
 */
struct node {
	listint_t **head;
};

#endif
